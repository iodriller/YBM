{
  "5c845cc4204c80a2": {
    "method": "generate_structured",
    "response": {
      "approval_gates": [],
      "assumptions": [
        "The code interpreter can generate and run Python scripts that write files.",
        "The user wants to know the location of the generated file."
      ],
      "id": "interpreter-report-plan",
      "objective": "Use the local code interpreter to create a file named interpreter-report.txt with a two-line summary of this test, run the script, and tell me where the file is.",
      "postconditions": [
        {
          "description": "interpreter-report.txt is delivered to the user.",
          "required": true,
          "type": "artifact_delivered"
        }
      ],
      "required_capabilities": [
        "terminal.run",
        "telegram.send"
      ],
      "steps": [
        {
          "description": "Use the code interpreter to generate and run a script that creates interpreter-report.txt with a two-line summary of this test.",
          "expected_output": "A file named interpreter-report.txt is created in the workspace directory.",
          "id": "step-1",
          "required_capabilities": [
            "terminal.run"
          ],
          "requires_approval": false,
          "risk_level": "high",
          "title": "Generate and run script to create interpreter-report.txt",
          "tool_input": {
            "objective": "Create a file named interpreter-report.txt with the following two lines: 'Test: Interpreter file creation.' and 'Result: File successfully created by code interpreter.'",
            "operation": "generate_and_run"
          },
          "tool_name": "code.interpreter"
        },
        {
          "description": "Send the generated interpreter-report.txt file to the user so they know where it is.",
          "expected_output": "The user receives the interpreter-report.txt file and can see its location.",
          "id": "step-2",
          "required_capabilities": [
            "telegram.send"
          ],
          "requires_approval": false,
          "risk_level": "low",
          "title": "Deliver interpreter-report.txt file location to user",
          "tool_input": {
            "operation": "send_file",
            "path": "interpreter-report.txt"
          },
          "tool_name": "artifact.deliver"
        }
      ],
      "success_criteria": [
        "interpreter-report.txt is created with the correct two-line summary.",
        "The user receives the file and knows its location."
      ]
    },
    "system_prompt": "You are the planning layer for a local agent-control bot.\nReturn only structured JSON matching the requested schema. No prose outside the JSON.\n\n## Your job\nProduce an ordered execution plan for the user's objective using only the tools\nlisted in the configuration context that follows. Use multiple steps when the\nwork requires it; one step is fine when it doesn't.\n\n## The tools\n\nThe runtime catalog appears below the objective. Each tool description lists\nits operations and worked example `tool_input` payloads. **Imitate those\nexamples**. Use ONLY tool names and operations that appear in that catalog.\nNever invent a tool name. If you don't see the tool you wanted in the catalog,\npick the closest one that exists \u2014 most user requests fit `browser.open`,\n`browser.control`, `filesystem.manage`, `http.request`, `code.interpreter`,\n`document.manage`, `artifact.deliver`, `computer.use`, or `task.status`.\n\n## `tool_name` vs `required_capabilities`\n\nThese are DIFFERENT and easy to confuse:\n\n- `tool_name` is which tool to call. Pick from the catalog (e.g. `artifact.deliver`).\n- `required_capabilities` is the low-level permissions the tool needs. Pick from\n  this exact list \u2014 nothing else:\n\n  `telegram.receive`, `telegram.send`, `llm.generate`, `stt.transcribe`,\n  `tts.synthesize`, `vscode.read_state`, `vscode.write_files`, `terminal.run`,\n  `filesystem.read`, `filesystem.write`, `desktop.screenshot`, `desktop.control`,\n  `browser.open`, `browser.control`, `network.http`, `schedule.manage`, `github.read`,\n  `github.push`, `dependencies.install`.\n\nTool \u2192 capability it needs (most common):\n- `artifact.deliver` \u2192 `telegram.send`\n- `filesystem.manage` read ops \u2192 `filesystem.read`; write ops \u2192 `filesystem.write`\n- `browser.open` \u2192 `browser.open`; `browser.control` \u2192 `browser.control`\n- `http.request` \u2192 `network.http`\n- `code.interpreter` \u2192 `terminal.run`\n- `computer.use` \u2192 `desktop.control`\n- `task.status` \u2192 `llm.generate`\n\nIf you put a tool name in `required_capabilities` (like `artifact.deliver`), the\nschema will reject it.\n\n## Step shape \u2014 required fields\n\nEach item in `steps` MUST have:\n- `title` \u2014 short label, e.g. \"Open dizibox homepage\"\n- `description` \u2014 one sentence on what this step does\n- `tool_name` \u2014 exact name from the runtime catalog\n- `tool_input` \u2014 object with `operation` and any other fields the example shows\n\nDo not add `success_criteria`, `validation`, `notes`, `comments`,\n`expected_result`, `prerequisites`, `metadata`, `inputs`, or `outputs` to a\nstep \u2014 the schema rejects them.\n\n## Paths and filesystem locations\n\nWhen the user mentions \"my desktop\", \"Documents\", or \"Downloads\" as a folder,\nuse the alias string \u2014 `\"desktop\"`, `\"documents\"`, `\"downloads\"` \u2014 in the\n`root` or `folder_path` field. The adapter resolves the actual user home.\n\nNEVER write a literal Windows path like `C:\\Users\\me\\...`, `C:\\Users\\user\\...`,\n`C:\\for fun\\...`, or any path with placeholder usernames. You do not know the\nreal username; the adapter does.\n\nIf a prior step found a file, reference it with `{{last_entry_path}}` rather\nthan guessing the path.\n\n## Cross-step data flow\n\nTo use the previous step's output in the next step, use one of these literal\nplaceholders in `tool_input` \u2014 the worker substitutes them at execution time:\n\n- `{{last_output}}` \u2014 text/summary the previous step produced\n- `{{last_manifest}}` \u2014 manifest array (e.g. file entries from a search)\n- `{{last_entry_path}}` \u2014 path of the previous step's primary result file\n- `{{workspace_dir}}` \u2014 the task's workspace directory\n\nDo not invent your own placeholder syntax.\n\n`code.interpreter` automatically registers files it creates as task artifacts.\nA following `artifact.deliver` step can reference them by basename\n(`path: \"sales_data.xlsx\"`) and the adapter finds them.\n\n## Routing shortcuts (pick the tool that fits \u2014 don't overthink)\n\n- Domain or URL in the request \u2192 `browser.open` (or `browser.control` if you need to click/fill).\n- JSON API / REST endpoint / exact API URL and `http.request` is enabled \u2192 `http.request`.\n- \"Tell me / show me / what's on my desktop or screen\" \u2192 `computer.use` with operation `observe`.\n- File on Desktop/Documents/Downloads \u2192 `filesystem.manage` with the alias.\n- Read or summarize a specific document (PDF/PPTX/DOCX) \u2192 `document.manage`.\n- Send a file or screenshot to the user \u2192 `artifact.deliver`.\n- Small calculation or local data transform \u2192 `code.interpreter` with `generate_and_run`.\n- \"Use Codex\" / \"use Copilot\" mentioned by name \u2192 `coding.agent` with `provider`.\n- Status of running tasks \u2192 `task.status`.\n- Schedule something recurring \u2192 `schedule.manage`.\n\nDo not use `code.interpreter` as a hidden network/browser fallback. It is for\nbounded local calculations, file generation, and local data transforms; network\naccess is disabled unless policy explicitly enables it.\n\n## Approval and timeouts\n\n- `requires_approval: false` for read operations.\n- `requires_approval: true` for write/control operations if policy requires it.\n- `timeout_seconds: 60` for simple ops; 120\u2013180 for browser / heavy ops.",
    "user_prompt": "Create an execution plan for this objective. Follow the rules in the system prompt and return JSON only.\n\n## Objective\nUse the local code interpreter to create a file named interpreter-report.txt with a two-line summary of this test, run the script, and tell me where the file is.\n\n\n## Available tools (only use names and operations from this catalog)\nAvailable worker tools:\n- workspace.manage: disabled; capability=filesystem.write; lifecycle=runtime; manage task workspaces under .agent_control/workspaces operations=prepare,write_files,materialize_static_app,launch_static,web_app_preview\n- filesystem.manage: disabled; capability=filesystem.write; lifecycle=runtime; inspect, search, plan organization, and apply move/copy manifests inside configured roots: .agent_control/workspaces operations=inspect_folder,search,resolve_desktop_item,find_by_description,open_file,read_file,write_text_file,collect_folder_snapshot,describe_folder,organize_plan,rename_plan,apply_manifest\n- adapter.factory: disabled; capability=filesystem.write; lifecycle=scaffold; scaffold, sandbox-test, and approval-promote generated adapter proposals under .agent_control/adapters operations=assess,scaffold,sandbox_execute_once,test_connector,promote_after_approval\n- code.interpreter: enabled; capability=terminal.run; lifecycle=runtime; generate and run bounded Python scripts through configured local/container backends under C:\\Users\\oneye\\AppData\\Local\\Temp\\ybm_scenario_scratch\\code_interpreter_generate_file operations=run_python,generate_and_run,solve_once,inspect_state,build_temp_helper,repair_script,health\n    example tool_input: {\"operation\": \"generate_and_run\", \"objective\": \"compute the 20th Fibonacci number and print it\"}\n    example tool_input: {\"operation\": \"generate_and_run\", \"objective\": \"write a Python script using openpyxl that creates sales_data.xlsx with sample sales rows\"}\n- http.request: disabled; capability=network.http; lifecycle=runtime; call allowlisted HTTP/REST APIs with optional secret injection; allowed targets: <none configured> operations=request\n- mcp.client: disabled; capability=terminal.run; lifecycle=runtime; discover and call configured external MCP server tools through stdio operations=discover,list_tools,call_tool,health,install_server\n- vscode.copilot_terminal: disabled; capability=vscode.write_files; lifecycle=runtime; send a prompt to VS Code/Copilot terminal or local Copilot CLI fallback\n- vscode.terminal_command: disabled; capability=vscode.write_files; lifecycle=runtime; queue an explicit terminal command through the VS Code bridge\n- coding_assistant: disabled; capability=terminal.run; lifecycle=runtime; run the configured local coding assistant command template\n- tts.synthesize: disabled; capability=tts.synthesize; lifecycle=runtime; synthesize local speech audio with the configured Kokoro ONNX runtime operations=synthesize\n- coding.agent: enabled; capability=terminal.run; lifecycle=runtime; start background Codex, Claude Code, or GitHub Copilot CLI sessions in a task workspace and report their status; completion is announced to the source chat automatically operations=start,plan,run_step,run_goal,status,limits,resume,stop,get_latest_output\n    example tool_input: {\"operation\": \"start\", \"provider\": \"codex\", \"prompt\": \"fix the failing tests in this repo\"}\n    example tool_input: {\"operation\": \"status\"}\n    example tool_input: {\"operation\": \"stop\", \"provider\": \"codex\"}\n- schedule.manage: disabled; capability=schedule.manage; lifecycle=runtime; create, list, pause, resume, delete, or run recurring task schedules operations=create,list,pause,resume,delete,run_now\n- task.status: enabled; capability=telegram.receive; lifecycle=runtime; report current task, plan, active, completed, and blocked state for status questions operations=status\n    example tool_input: {\"operation\": \"status\", \"limit\": 10}\n- artifact.deliver: enabled; capability=telegram.send; lifecycle=runtime; list task artifacts and deliver screenshots or files to the source Telegram chat operations=send_file,send_latest,send_screenshot,list_artifacts\n    example tool_input: {\"operation\": \"send_file\", \"path\": \"sales_data.xlsx\"}\n    example tool_input: {\"operation\": \"send_screenshot\"}\n    example tool_input: {\"operation\": \"send_latest\"}\n- document.manage: disabled; capability=filesystem.write; lifecycle=runtime; inspect documents, summarize PDFs, and create or revise PowerPoint files as task artifacts operations=inspect_document,extract_text,summarize_pdf,create_presentation,update_presentation\n- computer.use: disabled; capability=desktop.control; lifecycle=runtime; observe and control the local Windows desktop with bounded screenshot/action loops operations=observe,act,run_goal\n- browser.open: disabled; capability=browser.open; lifecycle=runtime; open Chrome, search the web, summarize exposed tabs/pages, and capture browser screenshots through DevTools at 127.0.0.1:9222 operations=open,search,research,inspect_tabs,screenshot,summarize_page,research_pages\n- browser.control: disabled; capability=browser.control; lifecycle=runtime; navigate, close tabs, click elements, and fill simple forms in Chrome through DevTools operations=navigate,close_tab,click,fill_form,check_page_update,extract_page_state,fill_form_step\n\n## Reminders\n- Risk: `low` for reads, `high` for writes / browser control, `critical` for desktop UI control.\n- For URLs/domains the user explicitly named: use `browser.open` with `operation: \"open\"` and the URL.\n- For \"find then read\" requests: two steps \u2014 `search` then `read_file` with `path: \"{{last_entry_path}}\"`.\n- For files on Desktop/Documents/Downloads: pass the alias (`\"desktop\"`, etc.) \u2014 never invent a literal path.\n- Multi-step when needed; one step when sufficient."
  },
  "5d0979f32211283c": {
    "method": "generate_structured",
    "response": {
      "code": "filename = \"interpreter-report.txt\"\nlines = [\n    \"Test: Interpreter file creation.\",\n    \"Result: File successfully created by code interpreter.\"\n]\n\nwith open(filename, \"w\", encoding=\"utf-8\") as f:\n    for line in lines:\n        f.write(line + \"\\n\")\n\nprint(f\"File '{filename}' created with the specified content.\")",
      "expected_files": [
        "interpreter-report.txt"
      ],
      "summary": "This script creates a file named 'interpreter-report.txt' with two specified lines of text."
    },
    "system_prompt": "You generate small Python scripts for a policy-gated code interpreter that may run locally or in a configured sandbox.\n\nRules:\n- Return structured JSON only.\n- Keep the script focused on the requested local task.\n- Use only Python standard-library modules that are safe for local data processing and not blocked by policy.\n- Do not use shell commands, network calls, package installation, subprocesses, OS mutation APIs, or absolute paths outside the workspace.\n- Write any generated files inside the provided workspace.\n- Print a concise final summary of what the script did.",
    "user_prompt": "Objective:\nCreate a file named interpreter-report.txt with the following two lines: 'Test: Interpreter file creation.' and 'Result: File successfully created by code interpreter.'\n\nWorkspace:\nC:\\Users\\oneye\\AppData\\Local\\Temp\\ybm_scenario_scratch\\code_interpreter_generate_file\\task_task_d943ad3b89da4e8fa33665e6016e20fc_801b3feb\n\nContext:\nNo extra context.\n\n\n\nReturn JSON matching this shape:\n{\n  \"summary\": \"short explanation of the script\",\n  \"code\": \"complete Python script text\",\n  \"expected_files\": [\"relative/path.txt\"]\n}"
  }
}