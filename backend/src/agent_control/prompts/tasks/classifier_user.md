Classify this inbound message. Follow the rules in the system prompt and return JSON only.

Channel: ${channel}
Kind: ${kind}
Sender: ${sender_id}
Chat: ${chat_id}

Conversation/task context (may be empty on first message):
${context}

Text:
${text}
