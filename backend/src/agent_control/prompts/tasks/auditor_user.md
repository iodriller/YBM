Objective (normalized): ${objective}

User's original message (preserve language and exact terms): ${original_message}

Raw tool output to audit:
${raw_output}

What this task has demonstrably produced (facts from the task record, not a judgment):
${deliverable_evidence}

Apply the sufficiency check first (count, topic, section), then the deliverable check. If sufficient, return the focused answer. If not, return INSUFFICIENT: <reason>.
