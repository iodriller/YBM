Objective (normalized): ${objective}

User's original message (preserve language and exact terms): ${original_message}

Raw tool output to audit:
${raw_output}

Apply the sufficiency check first (count, topic, section). If sufficient, return the focused answer. If not, return INSUFFICIENT: <reason>.
