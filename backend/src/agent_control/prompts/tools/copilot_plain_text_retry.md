The previous Copilot CLI attempt failed. Do not create, edit, or execute files.
Return the answer as plain text only, including complete fenced code blocks with filename=... metadata for every file and minimal run instructions.

Request: ${prompt}
